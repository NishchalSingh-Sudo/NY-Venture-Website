/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        // Keep your existing fonts if you still use them elsewhere
        inter: ['Inter', 'sans-serif'],
        playfair: ['"Playfair Display"', 'serif'], // Playfair is a serif, often used for display.

        // Add Roboto
        roboto: ['Roboto', 'sans-serif'], // Define your new font here
        // If you want to keep using 'font-roman' as the class name for Roboto:
        // roman: ['Roboto', 'sans-serif'],
      },
    },
  },
  plugins: [],
}